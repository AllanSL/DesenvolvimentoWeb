# Aula 01 — 03/02/2026

## Resumo
Primeiro encontro: apresentação dos elementos básicos de HTML e demonstração prática do professor. Sem tarefas cobradas.

## Conteúdos trabalhados
- Estrutura mínima de uma página (`index.html`, `<!DOCTYPE html>`, `<html>`, `<head>`, `<body>`)
- Elementos HTML comuns: títulos (`<h1>…<h6>`), parágrafos (`<p>`), links (`<a>`), listas (`<ul>`, `<ol>`, `<li>`)
- Boas práticas iniciais: semântica básica e organização de arquivos

## Arquivos
- `index.html`: página exemplo criada em aula.

## Como visualizar
- Abra `index.html` no navegador ou use o Live Server no VS Code.

## Extensões úteis no VS Code
- Live Server — servir páginas HTML localmente
- HTML CSS Support — autocomplete e dicas
- Prettier — formatação automática
- IntelliSense for CSS class names — sugestões de classes
